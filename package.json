{
  "name": "soccer-broadcast-api",
  "version": "1.0.0",
  "private": true,
  "description": "OndeAssistir Soccer Broadcast API (FastAPI + CLI)",
  "scripts": {
    "setup": "python app.py setup",
    "backfill": "python app.py backfill",
    "start": "python app.py serve"
  },
  "engines": {
    "node": ">=14"
  },
  "dependencies": {}
}
